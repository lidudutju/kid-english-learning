{
  "name": "@kel/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json",
    "migrate:local": "wrangler d1 migrations apply kel --local",
    "migrate:remote": "wrangler d1 migrations apply kel --remote",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@kel/shared": "workspace:*",
    "hono": "^4.6.10",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260904.1",
    "wrangler": "^4.129.0"
  }
}
